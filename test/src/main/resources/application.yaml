server:
  port: 8080
cat:
  prefix: catCATcatCAT
  suffix: qiuQIUqiuQIU
spring:
  profiles:
    active: dev
  cache:
    caffeine:
      spec:
    redis:
      time-to-live: 1000000
      cache-null-values: true
      key-prefix: 66

#  main:
#    #当遇到同样名字的时候，是否允许覆盖注册
#    allow-bean-definition-overriding: true
mybatis-encryptor:
  enable: true
  salt: 456sadas121d3a564das
  auto-clear: true

cache:
  timeout:
    redis:
      enable: true
    caffeine:
      enable: true